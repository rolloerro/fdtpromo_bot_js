# 🎯 fdtpromo_bot_js — RAG PDF Assistant

**Коротко.** fdtpromo_bot_js — лёгкий и надёжный Telegram‑бот для телепросмотра и ответов по сжатым PDF‑документам (например, `001_onco.pdf`). Работает по принципу Retrieval‑Augmented Generation (RAG): быстрый семантический/лексический поиск по документу + выдача выдержек пользователю в Telegram.

> *Коротко и по сути — документ в кармане врача: поиск, клипы и мгновенный доступ к справочной информации.*

---

## 🚀 Возможности (wow‑эффект)

* 📘 **Читает PDF** и извлекает текст — один раз при старте, кэширует для быстрого поиска.
* 🔎 **Лексический и семантический поиск** по предложениям — быстро находит нужные фрагменты в большом документе.
* 🧩 **RAG‑подход**: индекс + генерация контекста (легковесный RAG‑light), можно интегрировать в LangChain/embeddings позже.
* ✂️ **Умная выдача**: автоматическое разбиение длинных ответов на блоки (Telegram‑лимит 4096 символов) — никаких ошибок "message too long".
* 🔐 **Безопасность**: токены и секреты хранятся в `.env` (в репо не пушатся).
* ⚙️ Простая структура — можно быстро клонировать, запустить и интегрировать в монорепо Digital WM Core.

---

## 🧠 Технологический стек

* Node.js (ESM)
* Telegraf (Telegram bot framework)
* pdf‑parse (быстрое извлечение текста из PDF)
* (опционально) sentence‑transformers / OpenAI embeddings — для улучшения RAG
* GitHub Actions / Docker — CI/CD (шаблон в каталоге `.github` / `Dockerfile` — добавим по запросу)

---

## 📦 Структура репозитория

```
fdtpromo_bot_js/
├─ bot.js            # основной JS бот (Telegraf)
├─ pdf-parse.js      # утилиты для чтения PDF (wrapper для pdf-parse)
├─ docs/
│  └─ 001_onco.pdf   # (не пушить!)
├─ package.json
├─ .env.example      # пример переменных окружения
└─ README.md
```

---

## ⚡ Быстрый старт (локально)

1. Клонируй репо:

```bash
git clone https://github.com/rolloerro/fdtpromo_bot_js.git
cd fdtpromo_bot_js
```

2. Установи зависимости:

```bash
npm install
```

3. Создай `.env` в корне (не пушить):

```
BOT_TOKEN=123456789:AA...твой_токен
```

4. Положи нужный PDF в `docs/001_onco.pdf`.
5. Запусти бота:

```bash
node bot.js
```

После старта бот один раз прочитает PDF и будет готов к запросам в Telegram.

---

## 🔍 Как работает поиск

1. Бот извлекает текст и разбивает его на предложения/строки.
2. Для быстрого отклика используется простая индексация (split + lowercase + search).
3. Для повышения точности можно подключить эмбеддинги (OpenAI / HuggingFace) — предусмотрено место для интеграции.

---

## 🛡 Безопасность и рекомендации

* **Никогда** не пушь `.env` и большие закрытые PDF в публичный репозиторий — используй `.gitignore`.
* Для продакшна — хранить токены в секретах CI (GitHub Secrets, Docker secrets, Vault).
* Если добавляются эмбеддинги/LLM — следи за лимитами и приватностью данных пациентов.

---

## 💼 Для инвестора / партнёра — почему это важно

* Уникальное сочетание: клинический контент + быстрый доступ через мессенджер = снижение ошибок в экстренной помощи.
* Готовая инфраструктура Digital WM Core — с ней легко масштабировать: новые справочники, локализации, RAG‑сервисы.
* Низкий барьер входа: работает офлайн (PDF локально) + можно быстро подключить облачные LLM/embeddings для премиум‑функций.

---

## 🤝 Как можно помочь / куда двигаться дальше

* Добавить базовый RAG с эмбеддингами (OpenAI / HF) для семантического поиска.
* Добавить UI (Next.js) для просмотра найденных фрагментов и ссылок по страницам.
* Развивать серию ботов Digital WM: Resucitación, Shock, PANIC, FDT — единая платформа.

---

## ✉️ Контакты

**Владимир Копылов — Digital WM Core**

* Telegram: `@MSL72Rph`
* GitHub: `github.com/rolloerro`

---
